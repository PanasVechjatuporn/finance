
export const AssetSummaryGoalPieChart = ({ selectedData }) => {
    if(selectedData){
        selectedData.assets.forEach((asset) => {
            console.log('asset :: ',asset)
        })
    }
    return (
        <>
        </>
    )
}